{
  "name": "ems-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm run build && cd .. && rm -rf dist && mv client/dist ./dist"
  }
}
